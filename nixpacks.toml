[phases.setup]
nixPkgs = ['php83', 'php83Packages.composer']

[phases.install]
cmds = [
    'php --version'
]

[phases.build]
cmds = ['echo "Build phase - setup handled in start script"']

[start]
cmd = 'bash start_railway.sh'
