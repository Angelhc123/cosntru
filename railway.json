{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "apt-get update && apt-get install -y php8.3-mysql && composer install --no-dev --optimize-autoloader || echo 'No composer.json'"
  },
  "deploy": {
    "startCommand": "cd public && php -S 0.0.0.0:$PORT -t . router.php",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 60
  },
  "variables": {
    "MYSQL_ADDON_HOST": "${MYSQL_ADDON_HOST}",
    "MYSQL_ADDON_DB": "${MYSQL_ADDON_DB}",
    "MYSQL_ADDON_USER": "${MYSQL_ADDON_USER}",
    "MYSQL_ADDON_PORT": "${MYSQL_ADDON_PORT}",
    "MYSQL_ADDON_PASSWORD": "${MYSQL_ADDON_PASSWORD}",
    "APP_ENV": "production",
    "APP_DEBUG": "false"
  }
}
