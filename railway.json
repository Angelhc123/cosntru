{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash -c 'cd public && php -S 0.0.0.0:$PORT -t . router.php'",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 60
  }
}
